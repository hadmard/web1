- 添加一个栏目叫整木评选
- ~~添加广告位位置和广告位图片~~ → 已改为去掉所有广告位
- 不要叫主要栏目，去掉这几个字
- 添加一个主页位置叫十大推荐品牌（图标），并且可以点击进入品牌页面，具体内容先不用管
- **当前结构**：十大品牌置顶；每个大类首页单独呈现，大类下分小类，小类里是内容。
- **大类小类名称**：主账号在后台可修改；数据存库，seed 与 `lib/site-structure.ts` 仅作初始数据。
- **主账号与分管理员**：主账号可分配分管理员账号；分管理员做日常内容管理维护。详见 [产品规则](./PRODUCT_RULES.md)。

---

## 已完成（本次实现）

- **整木评选**：栏目 `/awards`，导航与页脚已加入「整木评选」。
- **去掉所有广告位**：首页与全站不再展示广告位。
- **十大推荐品牌置顶**：首页 Hero 下第一块即为十大推荐品牌，10 个入口点击进入 `/market`。
- **大类 → 小类 → 内容**：首页按「整木资讯、整木市场、整木词库、整木标准、整木数据、整木图库、整木评选、会员系统」八大类单独呈现，每块展示小类链接与「进入 XX」；各大类首页（如 `/news`、`/market`）统一用 `CategoryHome` 布局：面包屑、标题、描述、小类列表，小类链接到具体内容页（词条/标准/品牌/文章等）。配置在 `lib/site-structure.ts`。
- **WorldQuant 风格动画**：Hero 分步淡入、滚动提示、全幅金句与板块飞进保留。
