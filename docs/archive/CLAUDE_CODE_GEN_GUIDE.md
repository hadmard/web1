# 中华整木网 · Claude 核心代码生成完整指导

**文档用途**：本文件可直接整份复制给 Claude，用于生成「中华整木网」行业知识平台的核心代码架构。  
**重要定位**：本项目是「行业知识基础设施」，不是营销网站；风格须冷静、结构清晰、数据优先、极简，类似数据库/知识平台。

---

# 第一部分：战略与结构总结（统一认知版）

## 一、网站定位

中华整木网升级目标：

- 从「资讯门户」升级为「**整木行业知识基础设施 + 会员共建平台**」

核心原则：

- 以定义为中心
- 以标准为资产
- 以对比为流量入口
- 以数据为权威背书
- 以会员为共建体系

---

## 二、网站结构模型

采用「**双层结构**」：

### 第一层：公开知识层（行业基础设施）

| 栏目       | 说明               |
| ---------- | ------------------ |
| 整木词库   | 行业语义中心       |
| 整木标准   | 等级与规范系统     |
| 整木市场   | 品牌结构与对比系统 |
| 整木数据   | 行业数据与年鉴     |
| 整木资讯   | 趋势与政策         |
| 整木图库   | 结构图与风格图     |

### 第二层：会员共建层

- 标准共建
- 认证体系
- 数据共享
- 内部报告
- 专家委员会

---

## 三、内容结构规范（必须固化）

所有核心页面必须遵守以下结构。

### 1. 词条页结构

- 概念定义
- 发展背景
- 核心特征
- 技术结构
- 行业意义
- 相关词条
- 版本号

### 2. 标准页结构

- 标准编号
- 发布年份
- 等级划分
- 指标说明
- 应用范围
- 更新记录

### 3. 品牌页结构

- 品牌定位
- 材料体系
- 产品结构
- 价格区间
- 适合人群
- 商业模式
- 结构对比模块

### 4. 数据页结构

- 数据来源
- 统计方法
- 市场规模
- 区域分布
- 品类结构
- 更新周期

---

## 四、技术核心原则

网站必须具备：

- 结构化 Schema 数据
- 清晰层级 H1–H2–H3
- FAQ 模块
- 永久链接结构
- 版本号机制
- 可扩展数据库架构
- API 预留接口
- 中立表达，不带营销语言

---

# 第二部分：Claude 网站核心代码生成指导（可直接复制）

请为一个名为「中华整木网」的行业知识平台生成网站核心代码架构。

## 目标

构建一个行业知识基础设施网站，兼具公开知识系统与会员系统。

## 技术要求

- 使用 **Next.js + TypeScript**
- 使用 **TailwindCSS**
- 支持 **SSR**
- **SEO 友好**
- 支持 **Schema.org 结构化数据**
- 支持 **Markdown 内容渲染**
- 可扩展数据库（建议 **Prisma + PostgreSQL**）
- 模块化组件设计
- 支持会员登录系统（**JWT**）
- 预留 **API 接口**
- 可部署到 **Vercel**

---

## 一、信息架构

网站主要栏目：

| 路径           | 栏目说明   |
| -------------- | ---------- |
| `/dictionary`  | 整木词库   |
| `/standards`   | 整木标准   |
| `/market`      | 整木市场   |
| `/data`        | 整木数据   |
| `/news`        | 整木资讯   |
| `/gallery`     | 整木图库   |
| `/membership` | 会员系统   |

---

## 二、数据模型设计

请设计以下数据表。

### 1. Term（词条表）

- `id`
- `title`
- `slug`
- `definition`
- `background`
- `features`
- `structure`
- `significance`
- `version`
- `updatedAt`

### 2. Standard（标准表）

- `id`
- `title`
- `code`
- `year`
- `content`
- `version`
- `updatedAt`

### 3. Brand（品牌表）

- `id`
- `name`
- `positioning`
- `materialSystem`
- `productStructure`
- `priceRange`
- `targetAudience`
- `businessModel`
- `updatedAt`

### 4. IndustryData（行业数据表）

- `id`
- `title`
- `source`
- `methodology`
- `content`
- `year`
- `updatedAt`

### 5. Member（会员表）

- `id`
- `name`
- `email`
- `role`
- `membershipLevel`
- `passwordHash`

---

## 三、页面模板要求

### 词条页模板

- H1 标题
- 定义模块（突出展示）
- 分段结构
- FAQ 结构
- JSON-LD 结构化数据
- 版本号显示

### 标准页模板

- 标准编号突出显示
- 表格化等级系统
- 更新记录区块
- 引用格式模块

### 品牌页模板

- 品牌信息卡片
- 结构对比区块
- 标签系统
- 可扩展对比功能

### 数据页模板

- 数据来源区块
- 可视化图表组件
- 年份筛选
- 下载按钮

---

## 四、SEO 结构

- 自动生成 `sitemap.xml`
- 自动生成 `robots.txt`
- Open Graph 支持
- 页面 meta 自动生成
- URL 规范化
- 结构化数据输出

---

## 五、可扩展能力

- 未来可接入 AI 搜索接口
- 预留内部搜索引擎接口
- 支持标签系统
- 支持内容版本管理
- 支持管理员后台

---

## 请生成

1. 完整项目目录结构  
2. Prisma schema  
3. 核心页面示例代码  
4. 示例 API 代码  
5. 示例结构化数据代码  
6. 组件设计规范  

---

## 最关键提醒

**本项目是「行业知识基础设施」，不是营销网站。**

风格必须：

- 冷静
- 结构清晰
- 数据优先
- 极简风格
- 类似数据库平台

---

## 最后一句

你不是在做一个网站。  
你是在做：**整木行业的「认知操作系统」。**
