name: Setup SEO News Cron

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: SEO auto generation disabled
        shell: bash
        run: |
          echo "::error::SEO auto generation has been disabled. Cron installation is blocked until the article source isolation fix is accepted."
          exit 1
